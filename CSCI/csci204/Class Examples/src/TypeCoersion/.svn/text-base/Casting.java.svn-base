package TypeCoersion;

import Inheritance.Car;
import Inheritance.Sedan;

// Type casting AKA type coersion AKA type conversion
public class Casting {

	public void example() {
		int x;
		double y;
		boolean z;
		
		// x = 3.14; // Java refuses to accidentally lose data
		x = (int) 3.14; // Do we get 3 or 4?
		System.out.println(x);
		
		y = 3; // Not a problem, ints are numbers too, just add info
		System.out.println(y);
		
		// z = 2; // Would 2 be true or false??? huh??
		// z = (boolean) 2; // Uhh... how would we convert it??
		// Warning: some other languages find a way to allow this
		
		// You will see Casting when we cover Generics
	}
	
	public static void main(String[] args) {
		Casting c = new Casting();
		c.example();
	}
}
